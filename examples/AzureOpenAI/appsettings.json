{
  "Llm": {
    "DefaultProvider": "azure/production",
    "Providers": {
      "azure/production": {
        "Provider": "azure",
        "ApiKey": "${AZURE_OPENAI_KEY}",
        "ApiBase": "${AZURE_OPENAI_ENDPOINT}",
        "DeploymentName": "${AZURE_OPENAI_DEPLOYMENT}",
        "Enabled": true
      }
    }
  }
}
